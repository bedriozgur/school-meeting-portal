# Firebase Integration Placeholder

Firebase client scaffolding now lives in `src/lib/firebase`.

This folder is kept only as a note for future service-level helpers. Firestore repositories should be implemented under `src/repositories/firestore` so routes remain coupled to repository interfaces, not Firebase SDK calls.
